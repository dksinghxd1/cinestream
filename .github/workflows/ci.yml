name: CI/CD Pipeline

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  build-client:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./client
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Clean install
        run: |
          rm -rf node_modules package-lock.json
          npm install
      - name: Fix react-scripts permissions
        run: chmod +x node_modules/.bin/react-scripts
      - name: Build
        run: npm run build
      - name: Test
        run: npm test --if-present


# Add deploy steps for Vercel/Netlify as needed
